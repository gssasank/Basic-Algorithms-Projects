The time complexity to find a prefix is O(n) as we have to search the entire trie for the words which start with
the letters we have provided in the interactive search bar.
Space complexity is also same for the same reason.
